    <ul class="nav justify-content-end">
        <%- if usuario_signed_in? %>
          <li class="nav-item">
            <button type="button" class="btn btn-info"><%= link_to "Salir", destroy_usuario_session_path %></button>
          </li>
        <% else %>
          <li class="nav-item">
            <button type="button" class="btn btn-info"><%= link_to 'Iniciar', new_usuario_session_path %></button>           
          </li>
          <li class="nav-item">
            <button type="button" class="btn btn-info"><%= link_to 'Registrarse', new_usuario_registration_path %></button>
          </li>
        <% end %>
    </ul>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>